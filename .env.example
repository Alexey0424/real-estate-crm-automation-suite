# Copy real values into .env.txt (gitignored). See the README.
FUB_API_KEY=
# For FUB webhook registration (Part A):
X_SYSTEM=AHB-Automation
X_SYSTEM_KEY=
# Optional — unlocks real Slack channel IDs:
SLACK_BOT_TOKEN=
# Optional — only if shipping Make + programmatic import:
MAKE_API_TOKEN=
